const router=require('express').Router();const c=require('../controllers/v2Controller');const {requireAuth,requireRoles}=require('../middleware/auth');
const {messageRateLimit,offerRateLimit}=require('../middleware/actionRateLimit');
const marketplace=require('../controllers/marketplaceController');
const {uploadJedneSlike}=require('../middleware/upload');
const {uploadRateLimit}=require('../middleware/actionRateLimit');
const {uploadPrivateDocument}=require('../middleware/privateUpload');
router.get('/nekretnine',c.properties);router.get('/nekretnine/mapa',c.propertyMap);
router.get('/nekretnine/:slug/slicne',c.similar);router.get('/nekretnine/:id/faq',c.faq);router.get('/nekretnine/:slug',c.propertyDetail);
router.get('/omiljene',requireAuth,c.favorites);router.post('/omiljene/:id',requireAuth,c.addFavorite);router.delete('/omiljene/:id',requireAuth,c.removeFavorite);
router.get('/sacuvane-pretrage',requireAuth,c.savedSearches);router.post('/sacuvane-pretrage',requireAuth,c.createSavedSearch);router.patch('/sacuvane-pretrage/:id',requireAuth,c.updateSavedSearch);router.delete('/sacuvane-pretrage/:id',requireAuth,c.removeSavedSearch);
router.get('/organizacije',c.organizations);router.get('/organizacije/moje',requireAuth,marketplace.myOrganizations);router.get('/organizacije/:slug',c.organization);router.post('/organizacije',requireAuth,c.createOrganization);
router.get('/moji-oglasi',requireAuth,marketplace.myListings);router.post('/moji-oglasi',requireAuth,marketplace.createDraft);router.get('/moji-oglasi/:id/editor',requireAuth,marketplace.editor);router.patch('/moji-oglasi/:id/draft',requireAuth,marketplace.saveDraft);router.post('/moji-oglasi/:id/submit',requireAuth,marketplace.submitDraft);router.get('/moji-oglasi/:id/statistika',requireAuth,marketplace.listingStats);router.post('/moji-oglasi/:id/fotografije',requireAuth,uploadRateLimit,uploadJedneSlike,marketplace.uploadDraftImage);router.patch('/moji-oglasi/:id/fotografije',requireAuth,marketplace.arrangeImages);router.delete('/moji-oglasi/:id/fotografije/:imageId',requireAuth,marketplace.removeDraftImage);
router.post('/beta-feedback',requireAuth,marketplace.feedback);
router.get('/organizacije/:id/clanovi',requireAuth,marketplace.orgMembers);router.post('/organizacije/:id/pozivi',requireAuth,marketplace.inviteMember);router.post('/organizacije/pozivi/prihvati',requireAuth,marketplace.acceptInvite);router.patch('/organizacije/:id/clanovi/:memberId',requireAuth,marketplace.updateMember);router.post('/organizacije/:id/prijenos-vlasnistva',requireAuth,marketplace.transferOwnership);router.patch('/organizacije/:id/oglasi/:listingId/agent',requireAuth,marketplace.assignListing);router.get('/organizacije/:id/analitika',requireAuth,marketplace.orgAnalytics);router.get('/organizacije/:id/dokumenti',requireAuth,marketplace.verificationDocuments);router.post('/organizacije/:id/dokumenti',requireAuth,uploadPrivateDocument,marketplace.uploadVerificationDocument);router.get('/organizacije/:id/dokumenti/:documentId/preuzmi',requireAuth,marketplace.downloadVerificationDocument);
router.get('/razgovori',requireAuth,c.conversations);router.post('/razgovori',requireAuth,messageRateLimit,c.createConversation);router.get('/razgovori/:id',requireAuth,c.conversation);router.post('/razgovori/:id/poruke',requireAuth,messageRateLimit,c.sendMessage);router.post('/razgovori/:id/faq',requireAuth,c.publishFaq);
router.patch('/razgovori/:id/status',requireAuth,marketplace.conversationStatus);router.patch('/razgovori/:id/procitano',requireAuth,marketplace.markConversationRead);router.patch('/faq/:id',requireAuth,marketplace.updateFaq);
router.get('/termini',requireAuth,c.appointments);router.post('/termini',requireAuth,c.createAppointment);router.patch('/termini/:id',requireAuth,marketplace.updateAppointmentV2);
router.get('/termini/:id/historija',requireAuth,marketplace.appointmentHistory);
router.get('/pregovori',requireAuth,marketplace.negotiations);router.post('/pregovori',requireAuth,offerRateLimit,marketplace.createNegotiation);router.post('/pregovori/:id/stavke',requireAuth,offerRateLimit,marketplace.addNegotiationItem);
router.get('/uporedi',marketplace.compare);router.get('/preporuke',requireAuth,marketplace.recommendations);router.get('/nekretnine/:slug/historija-cijene',marketplace.priceHistory);router.get('/vodici',marketplace.guides);router.get('/vodici/:slug',marketplace.guide);
router.get('/pregovaracke-ponude',requireAuth,c.offers);router.post('/pregovaracke-ponude',requireAuth,offerRateLimit,c.createOffer);router.patch('/pregovaracke-ponude/:id',requireAuth,offerRateLimit,c.updateOffer);
router.get('/obavijesti',requireAuth,c.notifications);router.get('/obavijesti/stream',requireAuth,c.notificationStream);router.patch('/obavijesti/:id/procitano',requireAuth,c.readNotification);router.post('/prijave',requireAuth,c.report);
const admin=requireRoles('ANALYST','MODERATOR','SUPER_ADMIN'),moderator=requireRoles('MODERATOR','SUPER_ADMIN');
router.get('/admin/dashboard',admin,c.adminDashboard);router.get('/admin/analitika',admin,marketplace.adminAnalytics);router.get('/admin/korisnici',admin,marketplace.adminUsers);router.get('/admin/korisnici/:id',admin,c.adminUser);router.patch('/admin/korisnici/:id',moderator,c.adminUpdateUser);router.get('/admin/aktivnosti',admin,marketplace.adminActivity);router.get('/admin/izvoz/:type(korisnici|oglasi|aktivnosti)',admin,marketplace.adminCsv);router.get('/admin/:kind(nekretnine|organizacije|moderation)',admin,c.adminQueue);router.patch('/admin/:kind(nekretnine|organizacije|moderation)/:id',moderator,c.adminDecision);
router.patch('/admin/beta/:kind(korisnici|organizacije)/:id',moderator,marketplace.adminBetaAccess);router.get('/admin/beta-feedback',admin,marketplace.adminBetaFeedback);
router.get('/admin/vodici',admin,marketplace.adminGuides);router.post('/admin/vodici',moderator,marketplace.createGuide);router.patch('/admin/vodici/:id',moderator,marketplace.updateGuide);
module.exports=router;
