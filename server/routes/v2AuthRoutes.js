const router=require('express').Router();const c=require('../controllers/v2AuthController');const {loginRateLimit}=require('../middleware/loginRateLimit');const {requireAuth}=require('../middleware/auth');
router.get('/session',c.session);router.post('/register',c.register);router.post('/login',loginRateLimit,c.login);router.post('/logout',c.logout);router.get('/verify-email',c.verifyEmail);router.post('/verify-email',c.verifyEmail);router.post('/email',requireAuth,c.setEmail);router.post('/forgot-password',c.forgotPassword);router.post('/reset-password',c.resetPassword);module.exports=router;
